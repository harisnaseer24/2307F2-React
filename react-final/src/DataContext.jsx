

//Context Api
// Create context (Creating or initializing the global state)

import { createContext } from "react";

export const Cart = createContext(["charger","handsfree"]);


// useContext (using global state)

// Provider  (changing value of global state)
